## 중요 계약

- `expected_strength_keywords`와 `expected_concern_keywords`는 소문자 정규화 후 `in` 검사로 JD 텍스트와 매칭됨 (`rule_based_evaluators.py` 기준) — 한국어 키워드는 반드시 JD 텍스트에 실제로 포함된 단어여야 한다
- fixture 수정 후 기존 테스트 (`pytest src/agent/evals/`)가 전부 통과해야 한다
- evaluator 로직의 점수 산출 방식 변경 없음
