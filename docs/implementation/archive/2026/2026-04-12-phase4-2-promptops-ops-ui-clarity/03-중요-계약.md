## 중요 계약

- `PromptOpsStatusResponse` 또는 loader 계층에서 UI 섹션 설명에 필요한 요약 필드 추가 검토
- golden dataset의 source of truth는 Git-tracked local JSON으로 유지
- 운영 화면은 golden dataset read-only snapshot만 제공하고 편집 경로는 repo/PR 기반으로 유지
- 관리자 CTA 문구는 기능 이름보다 운영 목적이 먼저 보이도록 작성
- 링크 값이 비어 있어도 UI가 깨지지 않도록 방어 유지
