## 검증

- `bash -n scripts/worktree_common.sh` 구문 오류 없음
- `bootstrap_web_env_for_worktree` 실행 후 `.env.development.local`에서:
  - `PORT=31xx` (Next.js, agent kind에 따른 값) 확인
  - `NEXT_PUBLIC_API_BASE_URL=http://localhost:81xx` 확인
- `WEB_ORIGIN` 미설정 환경에서도 `api_base_url`이 올바르게 생성되는지 확인
- `npm run test -- --run` (apps/web) 전체 통과 (`internal_loader.test.ts` 포함)
- `python3 scripts/implementation_docs.py validate` 통과
- PR description에 버그 수정 3개, 테스트 결과 명시
