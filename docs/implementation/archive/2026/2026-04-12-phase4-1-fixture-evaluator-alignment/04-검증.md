## 검증

- `job_eval_gold.json`의 모든 샘플에서 `inputs.job.jd_raw_text`가 한국어 JD 텍스트를 포함하는지 확인
- `expected_strength_keywords`의 각 키워드가 해당 샘플의 `jd_raw_text`에 실제로 등장하는지 확인 (소문자 매칭 기준)
- `pytest src/agent/evals/` 전체 통과
- `APP_ENV=development poetry run eval-jobs sync-dataset` 이후 iteration 실험 smoke test에서 한국어 fixture 기준으로 리포트가 생성되는지 확인
