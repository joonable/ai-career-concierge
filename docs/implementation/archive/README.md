# Archive

완료된 implementation plan package는 연도별 하위 디렉토리(`archive/<YYYY>/`)로 이동합니다.

- archive는 `python3 scripts/implementation_docs.py archive-plan <plan_id>`로만 수행합니다.
- 수동 이동 대신 스크립트를 사용해 `MILESTONE.md`, `TODO.md`, frontmatter 상태를 함께 갱신합니다.
